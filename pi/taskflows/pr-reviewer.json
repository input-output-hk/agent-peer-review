{
  "name": "pr-reviewer",
  "description": "Work the reviewer side: claim and complete every review requested from me, and decide what to do next about the pull requests I have already reviewed.",
  "version": 1,
  "concurrency": 4,
  "strictInterpolation": true,
  "args": {
    "autonomy": {
      "type": "enum",
      "values": ["propose", "auto"],
      "default": "propose",
      "description": "propose (the default) posts a recommendation and merges nothing. Reviewing itself never merges, so this flow only passes the value through for the record."
    }
  },
  "phases": [
    {
      "id": "discover",
      "type": "script",
      "run": ["node", ".pi/taskflows/pr-reviewer/discover.mjs"],
      "timeout": 300000
    },
    {
      "id": "process",
      "type": "map",
      "over": "{steps.discover.json}",
      "as": "item",
      "agent": "executor",
      "dependsOn": ["discover"],
      "idempotent": false,
      "optional": true,
      "timeout": 1800000,
      "idleTimeout": 600000,
      "context": [".pi/taskflows/pr-reviewer/instructions.md"],
      "contextLimit": 20000,
      "task": "You are handling one pull request on the reviewer side.\n\nPull request: {item.repo} #{item.number}\nKind: {item.kind}\nHead commit: {item.headSha}\nTitle: {item.title}\nURL: {item.url}\nAutonomy for this run: {args.autonomy}\n\nThe instructions file above is your contract. Follow the branch for this item's kind exactly: it names the only tools you may use and the single result line you must print last. Work on this pull request only.\n\nNever use gh, git, or any other command to review, approve, comment on, label, or merge."
    },
    {
      "id": "summarize",
      "type": "script",
      "run": ["node", ".pi/taskflows/pr-reviewer/summarize.mjs"],
      "input": "{steps.process.output}",
      "dependsOn": ["process"],
      "timeout": 60000,
      "final": true
    }
  ]
}
